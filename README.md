# pred-ass-2
Learning an unknown probability density function from real-world NO₂ air quality data using a Generative Adversarial Network (GAN). The model learns the distribution purely from samples, without assuming any parametric form, and approximates the learned PDF using Kernel Density Estimation (KDE).
